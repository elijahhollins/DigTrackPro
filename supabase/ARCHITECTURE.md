# Database Structure & RLS Architecture

## 🏗️ Database Schema Overview

```
┌──────────────────────────────────────────────────────────────────┐
│                         COMPANIES TABLE                           │
│  ┌──────────┬───────┬──────────────┬────────────┐                │
│  │    id    │ name  │ brand_color  │ created_at │                │
│  └────┬─────┴───────┴──────────────┴────────────┘                │
│       │                                                            │
│       │ Referenced by all tenant data tables via company_id       │
│       │                                                            │
└───────┼────────────────────────────────────────────────────────────┘
        │
        ├─────────┬──────────┬──────────┬──────────┬──────────┬─────────┐
        │         │          │          │          │          │         │
        ▼         ▼          ▼          ▼          ▼          ▼         ▼
    ┌───────┐ ┌──────┐  ┌─────────┐ ┌────────┐ ┌───────┐ ┌─────────┐ ┌─────────┐
    │PROFILES│ │ JOBS │  │ TICKETS │ │ PHOTOS │ │ NOTES │ │NO_SHOWS │ │JOB_PRINTS│
    │        │ │      │  │         │ │        │ │       │ │         │ │         │
    │• Users │ │• Jobs│  │• Dig    │ │• Site  │ │• Text │ │• Utility│ │• PDF/   │
    │• Roles │ │• Work│  │  tickets│ │  photos│ │  notes│ │  marks  │ │  images │
    │• Auth  │ │      │  │         │ │        │ │       │ │         │ │         │
    └───┬───┘ └──┬───┘  └────┬────┘ └────────┘ └───────┘ └─────────┘ └────┬────┘
        │        │           │                                              │
        │        └───────┬───┘                                              │
        │                │                                                  │
        ▼                ▼                                                  ▼
    ┌─────────────┐  ┌──────────────┐                            ┌──────────────┐
    │PUSH_SUBS    │  │COMPANY_INVITES│                           │PRINT_MARKERS │
    │             │  │               │                           │              │
    │• Web push   │  │• Onboarding  │                           │• Annotations │
    │• Endpoints  │  │• Tokens      │                           │• On prints   │
    └─────────────┘  └──────────────┘                           └──────────────┘
```

## 🔐 RLS Policy Architecture

### Three-Tier Security Model

```
┌─────────────────────────────────────────────────────────────────┐
│                    AUTHENTICATION LAYER                          │
│                  (Supabase Auth / auth.uid())                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   SECURITY-DEFINER FUNCTIONS                     │
│                   (Elevated Privileges)                          │
│                                                                  │
│  ┌──────────────────────┐  ┌────────────────────────┐          │
│  │ is_super_admin()     │  │ get_user_company_id()  │          │
│  │ Returns: boolean     │  │ Returns: UUID          │          │
│  └──────────────────────┘  └────────────────────────┘          │
│                                                                  │
│  ┌──────────────────────────────────────────────────┐          │
│  │ validate_invite_token(token UUID)                │          │
│  │ Returns: (company_id, company_name)              │          │
│  └──────────────────────────────────────────────────┘          │
│                                                                  │
│  ┌──────────────────────────────────────────────────┐          │
│  │ get_company_by_name(name TEXT)                   │          │
│  │ Returns: (company_id, company_name, brand_color) │          │
│  └──────────────────────────────────────────────────┘          │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                     RLS POLICY LAYER                             │
│                   (Row-Level Filtering)                          │
│                                                                  │
│  ┌────────────────────────────────────────────────┐            │
│  │  POLICY TYPE 1: Own Profile Access             │            │
│  │  ─────────────────────────────────             │            │
│  │  • allow_own_profile                           │            │
│  │  • allow_own_push_subscriptions                │            │
│  │                                                 │            │
│  │  WHERE: id = auth.uid()                        │            │
│  │  CHECK: id = auth.uid()                        │            │
│  └────────────────────────────────────────────────┘            │
│                                                                  │
│  ┌────────────────────────────────────────────────┐            │
│  │  POLICY TYPE 2: Tenant Isolation                │            │
│  │  ───────────────────────────────                │            │
│  │  • tenant_isolation_jobs                        │            │
│  │  • tenant_isolation_tickets                     │            │
│  │  • tenant_isolation_photos                      │            │
│  │  • tenant_isolation_notes                       │            │
│  │  • tenant_isolation_no_shows                    │            │
│  │  • tenant_isolation_job_prints                  │            │
│  │  • tenant_isolation_profiles                    │            │
│  │  • tenant_isolation_companies                   │            │
│  │                                                  │            │
│  │  WHERE: company_id = get_user_company_id()      │            │
│  │  CHECK: company_id = get_user_company_id()      │            │
│  └────────────────────────────────────────────────┘            │
│                                                                  │
│  ┌────────────────────────────────────────────────┐            │
│  │  POLICY TYPE 3: Super Admin Override            │            │
│  │  ────────────────────────────────               │            │
│  │  • super_admin_read_all_profiles                │            │
│  │  • super_admin_read_all_companies               │            │
│  │  • super_admin_update_companies                 │            │
│  │  • super_admin_manage_invites                   │            │
│  │                                                  │            │
│  │  WHERE: is_super_admin()                        │            │
│  │  CHECK: is_super_admin()                        │            │
│  └────────────────────────────────────────────────┘            │
│                                                                  │
│  ┌────────────────────────────────────────────────┐            │
│  │  POLICY TYPE 4: Bootstrap & Onboarding          │            │
│  │  ───────────────────────────────────            │            │
│  │  • allow_company_insert                         │            │
│  │  • mark_invite_used                             │            │
│  │                                                  │            │
│  │  WHERE: (various conditions)                    │            │
│  │  CHECK: true (allow all)                        │            │
│  └────────────────────────────────────────────────┘            │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow: User Query Lifecycle

### Regular User Accessing Data

```
1. USER QUERY
   SELECT * FROM tickets;
   
   │
   ▼
   
2. AUTH LAYER
   • Extract auth.uid() from JWT token
   • Validate session
   
   │
   ▼
   
3. SECURITY-DEFINER FUNCTION
   • Execute: get_user_company_id()
   • Returns: '123e4567-e89b-12d3-a456-426614174000'
   
   │
   ▼
   
4. RLS POLICY EVALUATION
   • Check: tenant_isolation_tickets
   • Filter: WHERE company_id = '123e4567-e89b-12d3-a456-426614174000'
   
   │
   ▼
   
5. RESULT
   • Returns ONLY tickets belonging to user's company
   • Other companies' tickets are invisible
```

### Super Admin Accessing Data

```
1. USER QUERY
   SELECT * FROM tickets;
   
   │
   ▼
   
2. AUTH LAYER
   • Extract auth.uid() from JWT token
   • Validate session
   
   │
   ▼
   
3. SECURITY-DEFINER FUNCTION
   • Execute: is_super_admin()
   • Check: profiles.role = 'SUPER_ADMIN'
   • Returns: TRUE
   
   │
   ▼
   
4. RLS POLICY EVALUATION
   • Check: super_admin_read_all_companies
   • Filter: (none - access granted to all)
   
   │
   ▼
   
5. RESULT
   • Returns ALL tickets from ALL companies
   • Complete cross-tenant visibility
```

## 🛡️ Security Boundaries

```
┌───────────────────────────────────────────────────────────────┐
│                        PUBLIC INTERNET                         │
└───────────────────────────┬───────────────────────────────────┘
                            │
                            │ HTTPS / JWT Token
                            │
┌───────────────────────────▼───────────────────────────────────┐
│                      SUPABASE AUTH                             │
│  • JWT Validation                                              │
│  • Session Management                                          │
│  • User Identity (auth.uid())                                  │
└───────────────────────────┬───────────────────────────────────┘
                            │
                            │ Authenticated Context
                            │
┌───────────────────────────▼───────────────────────────────────┐
│                   POSTGRES DATABASE                            │
│                                                                │
│  ┌──────────────────────────────────────────────┐            │
│  │          ROW LEVEL SECURITY                  │            │
│  │  ┌────────────────────────────────────┐      │            │
│  │  │   Company A Data                   │      │            │
│  │  │   • User can see: ✅               │      │            │
│  │  │   • User can modify: ✅            │      │            │
│  │  └────────────────────────────────────┘      │            │
│  │                                               │            │
│  │  ┌────────────────────────────────────┐      │            │
│  │  │   Company B Data                   │      │            │
│  │  │   • User can see: ❌               │      │            │
│  │  │   • User can modify: ❌            │      │            │
│  │  └────────────────────────────────────┘      │            │
│  │                                               │            │
│  │  ┌────────────────────────────────────┐      │            │
│  │  │   Own Profile                      │      │            │
│  │  │   • User can see: ✅               │      │            │
│  │  │   • User can modify: ✅            │      │            │
│  │  └────────────────────────────────────┘      │            │
│  └──────────────────────────────────────────────┘            │
└───────────────────────────────────────────────────────────────┘
```

## 📊 Performance: Index Strategy

### Indexes on Company-Scoped Tables

```
TABLE: tickets
─────────────────────────────────────
│ Row 1: company_id = 'A'           │ ◄─── idx_tickets_company_id
│ Row 2: company_id = 'A'           │
│ Row 3: company_id = 'B'           │
│ Row 4: company_id = 'A'           │
│ Row 5: company_id = 'C'           │
─────────────────────────────────────

Query: SELECT * FROM tickets WHERE company_id = 'A'
Performance: O(log n) - Index lookup
Result: Rows 1, 2, 4 returned instantly
```

### Without Index (Before Migration)

```
TABLE: tickets
─────────────────────────────────────
│ Row 1: company_id = 'A'           │
│ Row 2: company_id = 'A'           │
│ Row 3: company_id = 'B'           │ ◄─── Scans every row
│ Row 4: company_id = 'A'           │
│ Row 5: company_id = 'C'           │
─────────────────────────────────────

Query: SELECT * FROM tickets WHERE company_id = 'A'
Performance: O(n) - Full table scan
Result: 10x-100x slower on large tables
```

## 🚦 Query Execution Plan

### Optimized Query (With Indexes)

```
EXPLAIN ANALYZE SELECT * FROM tickets 
WHERE company_id = get_user_company_id();

┌─────────────────────────────────────────────────────┐
│ Index Scan using idx_tickets_company_id             │
│   Rows: 42                                           │
│   Time: 0.05ms                                       │
│   Buffer hits: 3                                     │
└─────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────┐
│ Filter: company_id = (SELECT company_id             │
│         FROM profiles WHERE id = auth.uid())        │
│   Rows removed: 0                                    │
│   Time: 0.02ms                                       │
└─────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────┐
│ RESULT: 42 rows in 0.07ms total                     │
└─────────────────────────────────────────────────────┘
```

## 🎯 Policy Priority & Precedence

When multiple policies apply to a row:

```
┌────────────────────────────────────────────────────┐
│  1. PERMISSIVE Policies (default)                  │
│     Any one PERMISSIVE policy grants access        │
│     (OR logic)                                      │
└────────────────────────────────────────────────────┘
                     │
                     ▼
           ┌─────────────────┐
           │   Row Visible   │
           │   if ANY match: │
           │                 │
           │   ✓ Own profile │
           │   OR            │
           │   ✓ Same company│
           │   OR            │
           │   ✓ Super admin │
           └─────────────────┘
```

### Example: User Viewing Profiles Table

```
User ID: user-123
Company ID: company-A
Role: CREW

Profiles Table:
┌─────────┬────────────┬──────┬────────────────┐
│ User ID │ Company ID │ Role │ Visible?       │
├─────────┼────────────┼──────┼────────────────┤
│ user-123│ company-A  │ CREW │ ✅ (own profile)│
│ user-456│ company-A  │ ADMIN│ ✅ (same company)│
│ user-789│ company-B  │ CREW │ ❌ (different)  │
│ user-999│ company-A  │ CREW │ ✅ (same company)│
└─────────┴────────────┴──────┴────────────────┘

Policies Applied:
1. allow_own_profile        → TRUE for user-123 only
2. tenant_isolation_profiles → TRUE for company-A users
3. super_admin_read_all     → FALSE (not super admin)

Result: Can see rows for user-123, user-456, user-999
```

## 🔍 Security Audit Trail

### What RLS Protects Against:

```
┌────────────────────────────────────────────────────┐
│ ✅ PROTECTED                                       │
├────────────────────────────────────────────────────┤
│ • SQL Injection attacks                            │
│ • Direct database access                           │
│ • API bypasses                                     │
│ • Malicious application code                       │
│ • Compromised API keys                             │
│ • Cross-tenant data leaks                          │
│ • Unauthorized data modifications                  │
└────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────┐
│ ⚠️ NOT PROTECTED (Requires App Logic)             │
├────────────────────────────────────────────────────┤
│ • Business logic bugs in application               │
│ • User permission hierarchies within company       │
│ • Field-level permissions (e.g., salary data)      │
│ • Time-based access controls                       │
│ • Audit logging and compliance                     │
└────────────────────────────────────────────────────┘
```

## 📈 Scaling Characteristics

```
Database Size vs Query Performance (with indexes):

Performance (ms)
    ▲
 500│                                    
    │                                    ─ ─ Without Indexes
 400│                              ─ ─ ─
    │                        ─ ─ ─
 300│                  ─ ─ ─
    │            ─ ─ ─
 200│      ─ ─ ─
    │─ ─ ─
 100│━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  With Indexes
    │
   0└────────────────────────────────────────────────────▶
     1K    10K   100K   1M    10M   Rows

Key Insight: With proper indexing, query time remains
constant regardless of database size (O(log n))
```

---

**Visual Guide Version:** 1.0  
**Last Updated:** 2026-02-22  
**Compatible With:** complete_rls_setup.sql v1.0
